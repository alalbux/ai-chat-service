# Swagger / OpenAPI

When the API is running:

- **Swagger UI:** `http://localhost:3000/docs` (or your deployed host).
- **OpenAPI JSON:** `http://localhost:3000/docs-json`

The document is generated from Nest decorators (`@nestjs/swagger`). Keep route summaries aligned with `packages/contracts` so clients and docs stay consistent.
